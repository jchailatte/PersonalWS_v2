import { Fragment, useMemo } from 'react';
import PropTypes from 'prop-types';

const HUDText = (props) => {

    return (
        <Fragment>
        </Fragment>
    );
};

HUDText.propTypes = {
};

HUDText.defaultProps = {
}

export default HUDText;
